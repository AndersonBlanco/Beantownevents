# Taloring Next JS to class guidelines 
* removed layout.js file in order to peroprly require the html skeleton setup (<html/>, <body/>, etc) as part of manual implementation for each page 
* Cleared default CSS file content that came prepakcaged with Next JS to build our own styling from scratch 
* created external package-lock and package.json files with npm init -y to track next js libary (create-next-app) which was used to inititalize the project structure 

