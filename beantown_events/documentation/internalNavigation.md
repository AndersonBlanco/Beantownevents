# Internal Navogation strucutre: 
* Use href = "/folderName" to reference a specific route or navigation. This will execute the js file in that folder and return the content that the js file's function returns (the borwser executes that file - the web is primarly runs on JS so we can do this browser)
* Alternaitve: <Link to = "/folderName"/>

